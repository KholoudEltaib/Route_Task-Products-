name: rout_task_products
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.0
  dio: ^4.0.0
  get_it: ^7.0.3
  injectable: ^2.0.0
  http: ^1.1.0
  equatable: ^2.0.5
  dartz: ^0.10.1
  bloc: ^8.1.4
  provider: 6.1.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.0.0
  build_runner: ^2.1.0

flutter:
  uses-material-design: true
